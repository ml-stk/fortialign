name: Deploy FortiAlign Batch 7

on:
  push:
    branches:
      - feat/fortialign-v2-batch7
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: fortialign-batch7-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Batch 7
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Publish dist to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./dist
          force_orphan: false
          commit_message: "Deploy FortiAlign Batch 7 ${{ github.sha }}"
